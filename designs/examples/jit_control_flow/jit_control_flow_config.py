from __future__ import annotations

DEFAULT_PARAMS = {
    'rounds': 4
}

TB_PRESETS = {
    "smoke": {"timeout": 64, "finish": 1},
    "nightly": {"timeout": 256, "finish": 16},
}

SIM_TIER = "normal"
