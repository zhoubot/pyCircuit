from __future__ import annotations

DEFAULT_PARAMS = {
    'width': 32
}

TB_PRESETS = {
    "smoke": {"timeout": 64, "finish": 2},
    "nightly": {"timeout": 256, "finish": 16},
}

SIM_TIER = "normal"
