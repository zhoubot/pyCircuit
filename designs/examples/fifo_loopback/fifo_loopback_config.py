from __future__ import annotations

DEFAULT_PARAMS = {
    'depth': 2
}

TB_PRESETS = {
    "smoke": {"timeout": 64, "finish": 3},
    "nightly": {"timeout": 256, "finish": 16},
}

SIM_TIER = "normal"
